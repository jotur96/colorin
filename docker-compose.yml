services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - ./data:/app/data
    environment:
      - DATABASE_PATH=/app/data/colorin.db
    working_dir: /app
    restart: unless-stopped

